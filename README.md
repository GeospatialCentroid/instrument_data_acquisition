# instrument_data_acquisition

## setup

```python3 -m venv venv
source venv/bin/activate

```
```
pip install -r requirements.txt 
```
or to skip errors
```
cat requirements.txt | xargs -n 1 pip install
```